<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Battleship by IL</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
    integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <!-- <link rel='stylesheet' href='../styles/main.css'> -->
  <style>
    .board-node {
      background-color: skyblue;
      width: 40px;
      height: 40px;
      margin: 0;
      padding: 0;
    }

    .myTable {
      background-color: blue;
      width: 100%;
      height: 100%;
      margin: 0 auto;
      border: 1px solid black;
    }

    .myContainer {
      width: 180px;
      height: 180px;
      display: block;
      text-align: center;
    }

    .myRow {
      height: 10%;
      width: 100%;
      border: 1px solid black;
    }

    .myData {
      height: 20px;
      width: 20px;
      border: 1px solid black;
    }
  </style>
</head>

<body>
  <header>
    <nav class="navbar navbar-expand-md navbar-dark bg-info" style='padding: 28px 10px'>
      <a class="navbar-brand" href="/">Battleship</a>
    </nav>
  </header>
  <table>
    <thead>
      <strong>Game Replay:</strong></thead>

    <% for(let i = 0; i < battleLog.length; i++) {%>
    <tr>
      <td>
        Turn <%= battleLog[i].turn%>
        <%= battleLog[i].desc%>

      </td>

      <td>
        <div class='myContainer'>
          <table class='myTable'>
            <thead>Player 1:
            </thead>
            <% for(let row in battleLog.boardState['Player 1']) { %>
            <tr class='myRow'>
              <% for(let node in battleLog.boardState['Player 1'][row]) { %>
              <td class='myData' id=<%=row + node%>>
                <% if(battleLog.boardState['Player 1'][row][node] === 0) {%>
                -
                <%} else {%>
                <%=battleLog.boardState['Player 1'][row][node] %>
                <% }%>
              </td>
              <% } %>
            </tr>
            <% } %>
          </table>
        </div>
      </td>
      <td>
        <div class='myContainer'>
          <table class='myTable'>
            <thead>Player 2:
            </thead>
            <% for(let row in battleLog.boardState['Player 2']) { %>
            <tr class='myRow'>
              <% for(let node in battleLog.boardState['Player 2'][row]) { %>
              <td class='myData' id=<%=row + node%>>
                <% if(battleLog.boardState['Player 2'][row][node] === 0) {%>
                -
                <%} else {%>
                <%=battleLog.boardState['Player 2'][row][node] %>
                <% }%>
              </td>
              <% } %>
            </tr>
            <% } %>
          </table>
        </div>
      </td>
    </tr>
  </table>
  <% } %>
  <!-- <table class='board table table-bordered' id=<%=number%> >
    <thead>Opponent's Board: </thead>
    <% for(let row in user.opBoard) { %>
    <tr class='board-row'>
      <% for(let node in user.opBoard[row]) { %>
      <td class='board-node' id=<%=row + node%>>
        <form method='POST' action='/battle/<%=row + node%>?_method=PUT'>
          <button style="height:100%; width:100%;" class='btn-info'>
            <% if(user.opBoard[row][node] === 0) {%>
            -
            <%} else {%>
            <%=user.opBoard[row][node] %>
            <% }%>
          </button>
        </form>
      </td>
      <% } %>
    </tr>
    <% } %>

  </table> -->
  <div>
    <form class='text-center' name='ready'>
      <button id='btnReady' class='btn-danger' autofocus></button>
    </form>
  </div>
</body>

</html>